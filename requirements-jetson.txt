# Jetson AGX Orin (JetPack 6 / L4T R36.4, CUDA 12.6) 추론용.
#
# torch는 PyPI가 아닌 NVIDIA Jetson wheel로 설치해야 한다 (pip -r 로 자동 설치 불가):
#   1순위: pip install torch --index-url https://pypi.jetson-ai-lab.io/jp6/cu126
#   폴백 : pip install --no-cache-dir \
#     https://developer.download.nvidia.com/compute/redist/jp/v61/pytorch/torch-2.5.0a0+872d972e41.nv24.08.17622132-cp310-cp310-linux_aarch64.whl
# TensorRT는 현재 JetPack 환경에 이미 제공된 경우에만 사용한다. 없으면 tiny+ORT를 사용한다.
# 이 저장소 작업 중 apt/sudo 설치는 금지하며, 유저 공간 구성은 scripts/jetson/setup_jetson.sh 참조.
-r requirements.txt
sounddevice>=0.4.6
onnx>=1.15
# 1.19+ 는 Tegra(aarch64)에서 cpuinfo 크래시 (세션 생성 시 abort) — 1.18.1 로 고정
onnxruntime==1.18.1
cuda-python>=12.0
tensorboard>=2.15
pytest>=7.4
